# OptiMaster

OptiMaster is a local audio finishing assistant for WAV and FLAC premaster files.

It analyzes a source file, renders several mastering candidates with FFmpeg, measures the results, then helps you compare and export the best version. It is designed for cautious, repeatable finishing work, not for one-click miracle mastering.

## Public Beta

Current beta version: `2026.4.24`.

This project is in beta and is meant to be tested.

Expect rough edges, especially around packaging, edge-case audio files, and UI polish. The app is useful today for local experiments, but you should still verify exports with your normal monitoring chain before release.

Good beta feedback includes:
- files that fail to analyze or render
- moments where the workflow feels unclear
- candidate choices that sound worse than expected
- confusing loudness or true-peak results
- Windows packaging and shortcut/icon issues

## What It Does

OptiMaster currently supports:
- WAV / FLAC import
- source loudness analysis with FFmpeg
- source profile diagnostics
- focused Source / Versions / Listen-Export workflow
- Clean / safe, Balanced master, and Push louder optimization modes
- quick LUFS targets for streaming, SoundCloud, club/DJ, hard/raw tests, and extreme loudness checks
- auto-recommended LUFS target after source analysis
- custom target LUFS in the GUI with a non-editable LUFS unit
- loudest-clean search for users who want maximum level while keeping quality
- strict true-peak option
- multiple rendered candidates per session
- an OptiMaster technical fallback when a custom LUFS target is too aggressive
- ranked recommendations with scoring reasons
- before / after comparison for LUFS, true peak, dynamics, and score
- A/B playback inside the GUI
- waveform preview and playback visualization
- render progress with an estimated time remaining
- render cancellation from the GUI
- export with clean incremental filenames
- local session history
- CLI usage for analysis, rendering, batch processing, presets, and listening notes

## What It Is Not

OptiMaster does not replace a mastering engineer.

It does not guarantee a release-ready master, does not know your creative intent, and does not judge translation on real playback systems. It gives you controlled candidates and useful measurements so you can make a better decision.

## Requirements

- Windows is the primary packaged target right now.
- FFmpeg must be available on `PATH`.
- Source installs require Python 3.11+ and a local checkout of this repository.

Python dependencies are declared in [pyproject.toml](pyproject.toml). The GUI uses PySide6.

## Download Beta

Public beta builds are published from Git tags through GitHub Releases:

<https://github.com/MLyte/OptiMaster/releases>

For non-developers, download the latest Windows portable archive from the project releases page:

```text
OptiMaster-v2026.4.24-beta.1-windows-x64.zip
```

The `.zip` is a file to download, not a PowerShell command. Download it from GitHub Releases, then unzip it:

```powershell
Expand-Archive .\OptiMaster-v2026.4.24-beta.1-windows-x64.zip -DestinationPath .\OptiMaster-release -Force
.\OptiMaster-release\OptiMaster.exe
```

If the archive contains an `OptiMaster` folder, launch the executable inside it:

```powershell
.\OptiMaster-release\OptiMaster\OptiMaster.exe
```

Make sure FFmpeg is installed and available on your system `PATH` before analyzing audio.

Release archives are generated by GitHub Actions. They are not committed to the repository and there is no compiled branch.

## Release Build Status

The release workflow lives in [.github/workflows/release.yml](.github/workflows/release.yml).

To publish a beta build:

```powershell
git tag v2026.4.24-beta.1
git push origin v2026.4.24-beta.1
```

Then check GitHub Actions:

<https://github.com/MLyte/OptiMaster/actions>

When the workflow succeeds, the generated files are attached to GitHub Releases:

- `OptiMaster-v2026.4.24-beta.1-windows-x64.zip`
- `SHA256SUMS.txt`

## Install From Source

```powershell
py -3.11 -m venv .venv
.\.venv\Scripts\Activate.ps1
python -m pip install --upgrade pip
python -m pip install -e .
ffmpeg -version
```

If `ffmpeg -version` fails, install FFmpeg and make sure `ffmpeg.exe` is available on `PATH`.

## Launch The App

```powershell
optimaster-gui
```

Fallback:

```powershell
python -c "from optimaster.gui import run; raise SystemExit(run())"
```

## Recommended GUI Workflow

1. Choose a WAV or FLAC premaster.
2. Analyze the source.
3. Review the suggested LUFS target.
4. Choose a quick target or enable loudest-clean search.
5. Render candidates.
6. Watch render progress, ETA, or cancel if needed.
7. Compare the recommended version, your LUFS-target version, and the OptiMaster technical fallback.
8. Listen A/B.
9. Export the selected version.

The A/B check only unlocks after versions are created and a candidate is selected.

Suggested quick targets:

- `Clean streaming master (-14 LUFS)`: conservative, broadly safe for normalized streaming.
- `SoundCloud loud clean (-10.5 LUFS)`: louder and direct; good for SoundCloud-style playback.
- `Club / DJ loud (-9 LUFS)`: more pressure for DJ sets and club use.
- `Hard / raw test (-8 LUFS)`: aggressive test for hard techno/raw energy.
- `Extreme loudness check (-7 LUFS)`: stress test only; listen carefully before export.

If a requested LUFS target is too aggressive, OptiMaster still renders a cleaner technical fallback so you can compare instead of starting over.

Exports are suggested one folder above `renders` with incremental names such as:

```text
Track_OptiMaster_export_01.wav
```

## GUI Notes

- Buttons that are not available yet are disabled or replaced by explanatory status text.
- Long diagnostics and scoring explanations are wrapped for readability.
- The app icon and accent color are reused in the interface.
- The `Connect` menu links to the maker website: <https://mathieuluyten.be/>

## Release Smoke Test

Before calling a beta release good enough to share:

1. Download the release `.zip` from GitHub Releases.
2. Unzip it on Windows.
3. Launch `OptiMaster.exe`.
4. Confirm the app starts without a terminal window.
5. Import a WAV or FLAC file.
6. Analyze the source.
7. Create versions.
8. Confirm progress, ETA, and Cancel are visible during render.
9. Listen A/B.
10. Export the final file.
11. Start a new analysis without restarting the app.

## CLI Quick Start

Show help:

```powershell
optimaster --help
```

Analyze a file:

```powershell
optimaster analyze "C:\path\to\track.wav"
```

Render and score candidates:

```powershell
optimaster optimize "C:\path\to\track.wav" --output-dir ".\renders"
```

Choose a mode:

```powershell
optimaster optimize "C:\path\to\track.wav" --mode safe
optimaster optimize "C:\path\to\track.wav" --mode balanced
optimaster optimize "C:\path\to\track.wav" --mode louder
```

Batch process files:

```powershell
optimaster optimize-batch "C:\path\to\a.wav" "C:\path\to\b.wav" --output-dir ".\renders"
```

List built-in presets:

```powershell
optimaster presets
```

Use a YAML config:

```powershell
optimaster --config ".\config.example.yaml" optimize "C:\path\to\track.wav" --output-dir ".\renders"
```

Add a listening note:

```powershell
optimaster add-note transparent_trim --rating 4 --preferences ".\renders\preferences.json"
```

## Outputs

Each optimization session can produce:
- rendered candidate audio files
- `analysis.json`
- `ranking.json`
- optional `preferences.json`
- waveform preview images for the GUI

The rendered candidates in `renders` are working files. Use the GUI export action for a clean final filename.

## Packaging Status

Packaging is still beta. The current Windows packaging direction is documented in [WINDOWS_PACKAGING.md](WINDOWS_PACKAGING.md), and the source-only release policy is documented in [RELEASE.md](RELEASE.md).

The intended release build uses PyInstaller with the bundled OptiMaster icon and the GUI entrypoint. Do not build from `src/optimaster/__main__.py`; that is the CLI entrypoint.

```powershell
pyinstaller --noconfirm --windowed --name OptiMaster --icon src/optimaster/assets/optimaster_icon.ico --collect-all PySide6 src/optimaster/gui.py
```

## Testing

Run the test suite:

```powershell
python -m pytest -q
```

On some Windows setups, pytest may fail because the default temp directory is not writable. In that case, redirect temp files into the workspace:

```powershell
$env:TEMP="C:\www\OptiMaster\.tmp2"
$env:TMP="C:\www\OptiMaster\.tmp2"
New-Item -ItemType Directory -Force -Path .tmp2 | Out-Null
python -m pytest -q --basetemp .pytest-run -p no:cacheprovider
```

## Known Beta Notes

- FFmpeg must be installed separately for source runs.
- The GUI is Windows-first and still evolving.
- Target LUFS can be pushed too high; OptiMaster now renders a technical fallback so you can compare instead of restarting.
- Loudest-clean search renders more candidates and can take longer.
- ETA is an estimate based on current progress; it becomes more useful once rendering is underway.
- Loudness metrics are technical guidance, not a final listening decision.
- Session history and preferences are local files, not cloud-synced.

## License

OptiMaster is open source under the MIT License. See [LICENSE](LICENSE).
